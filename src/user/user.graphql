type User {
	id: ID!
    updateDate: Date!
	createDate: Date!
	username: String!
	name: String!
	todos: [Todo]
}

input CreateUserDto {
	username: String!
	password: String!
	name: String
}

type Query {
	user(id: Int!): User
}

type Mutation {
	createUser(createUserDto: CreateUserDto!): User
}
